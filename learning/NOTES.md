# Notes

- Workspace created 2026-08-15, via `/teach all about what we did in this project`.
- Goal (from user): internalize the chunked/resumable-transfer *pattern* well enough to reapply it in a different project/stack — not just recall this codebase.
- Preference: interleave concept lessons (protocol design, checksums, resumability, memory bounds) with concrete stack lessons (Spring Boot, React+shadcn, Docker/nginx deploy) rather than blocking them into two separate tracks.
- No stance yet from the user on joining a community (tus.io discussions, etc.) — ask rather than assume.
